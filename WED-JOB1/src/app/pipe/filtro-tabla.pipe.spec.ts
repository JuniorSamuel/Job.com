import { FiltroTablaPipe } from './filtro-tabla.pipe';

describe('FiltroTablaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTablaPipe();
    expect(pipe).toBeTruthy();
  });
});
